const number = 'number';
const text = 'text';

const increment = (state) => state + 1;
const decrement = (state) => state - 1;
const setValue = (state) => state;

export { number, text, increment, decrement, setValue };
